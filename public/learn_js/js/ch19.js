$(document).ready(() => {
  const $paras = $('p');
  console.log($paras);
  console.log(typeof $paras);
  console.log($paras instanceof $);
  console.log($paras instanceof jQuery);

  // 나머지 제이쿼리는 작업하면서 공부
});
